# IRIS 2.0 Offline Module — Python Requirements
# Install via: pip install -r requirements.txt
#
# NOTE: picamera2 is NOT listed here because it must be installed as a
# system package on Raspberry Pi OS, not via pip.
# Install it with: sudo apt install -y python3-picamera2

# ─── Core inference ──────────────────────────────────────────────────────────
ultralytics>=8.0.200         # YOLOv8n loader (also handles NCNN inference)
numpy>=1.24,<2.0             # Pinned <2.0 for ultralytics compat
# NOTE: ncnn is NOT installed via pip — no Python 3.13 ARM64 wheel exists on PyPI.
# It is built from source by setup.sh (one-time ~8 min build).

# ─── Computer vision ────────────────────────────────────────────────────────
# Use headless build on Raspberry Pi (no Qt dependency)
opencv-python-headless>=4.8

# ─── Web server ─────────────────────────────────────────────────────────────
flask>=3.0
werkzeug>=3.0
pyopenssl>=23.0              # HTTPS SSL context for Flask
cryptography>=41.0           # Required by pyopenssl

# ─── Hardware I/O ────────────────────────────────────────────────────────────
smbus2>=0.4                  # I2C for MPU9250
rpi-lgpio                    # GPIO for HC-SR04 — Pi 5 compatible RPi.GPIO drop-in
